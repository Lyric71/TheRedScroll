# Publication: xiaohongshu-business-account-setup

Brief 03B, "platform depth" slot, week 3. Row moved from `image_ready` to
`published`.

**This was a catch-up run.** The scheduled publish for 2026-09-22 04:00
Shanghai did not run, did not finish, or ended with an error, and
`Relaunch-MissedTasks.ps1` relaunched it. The run log
`logs/runs/2026-09-22-publish.txt` held only the two launcher lines, so there
was no partial publish work from an earlier model to repair and nothing valid
to preserve beyond the working tree. Model Opus, attempt 1 of the fallback
chain.

## Scope

`editorial/schedule.csv` holds exactly one row that is `image_ready` with a
`publish_date` of today or earlier: 03B, 2026-09-22,
`output/xiaohongshu-business-account-setup.md`, content type "Operational
how-to", which publishes as an insight. 03C (2026-09-24,
`chinese-entity-social-media`) and 03D (2026-09-25, `camper-china-teardown`)
are also `image_ready` but not yet due, and were left alone.

The row was read with a real CSV parser, not a field split. The `notes` column
is full of quoted commas, so `awk -F,` misreports the status of four rows. The
2026-09-21 publish log flagged this; it is still true.

The working tree carried the 2026-09-22 *draft* run's uncommitted artifacts:
the 03D output, its hero, `scripts/count-body.mjs`, the schedule and
source-ledger edits, `logs/2026-09-22.md`, and a `logs/scratch/` folder. Those
are valid completed work. The article files and the ledger edits ride along in
this commit on the same basis as the 2026-09-17 and 2026-09-21 publish commits.
`logs/scratch/` holds the two intermediate PNGs from the hero step and is not
committed, matching every earlier run.

Confirmed before starting that no
`src/content/blog*/xiaohongshu-business-account-setup.md` existed in any locale,
so the publish began from a clean state.

## Content

Source copy was taken verbatim. Only structural transformations were applied:

- The H1 became `title`, the hero standfirst became `description`.
- The source frontmatter `title` and `description` became `metaTitle` (40 of
  60) and `metaDescription` (140 of 155), the same mapping the previous ten
  insights use.
- HTML comment blocks (section markers, the feature-image instruction block,
  the schema note, the asset brief) were stripped.
- The trailing `CTA: Book a call` line was dropped, because
  `src/pages/insights/[...slug].astro` renders the closing call to action from
  the layout. The `## Book a call` H2 above it was kept, which is what
  `first-90-days-china-social-media` and `xiaohongshu-marketing-foreign-brands`
  both ship.
- The 16 `Source:` lines were each joined onto one line ending with the URL.
  The draft wraps them across three or four lines; every published article in
  the collection uses the single-line form.
- Five internal links were wired from the asset brief: the Xiaohongshu guide
  for foreign brands in Step 2, the RedNote agency page in Step 5, the China
  market entry service in the no-Chinese-entity section, the 90-day guide and
  the pricing page in the timeline section.

`category` is `Platforms`, matching `xiaohongshu-algorithm` and
`xiaohongshu-account-not-growing`. `platforms` is `["rednote"]`. The article
mentions WeChat and Alipay only in passing and never cites them, so no other
platform tag applies.

One word was changed rather than shipped verbatim. The draft's blockquote read
"A third-party review service partner, a named Shanghai information services
company". "Partner" is on the `STYLE_GUIDE.md` banned list, which
`editorial/CLAUDE.md` restates as absolute and which overrides the
createblogarticle verbatim rule. It is now "service provider", which renders
the platform's 第三方审核服务商 at least as accurately. Fixed in both the
published EN post and `editorial/output` so the two do not drift. The four
locales never carried the word.

"Country manager" was left alone. It is a job title, not the banned
"we manage your accounts" sense, and "manager" already appears in
`what-is-wecom` and `douyin-marketing-western-brands`.

The hero was already at
`public/images/blog/xiaohongshu-business-account-setup.webp` from the
2026-09-18 draft run (Suzhou converted-townhouse brand office, the Xiaohongshu
enterprise verification form legible on the laptop, a note grid on one phone
and a WeChat thread on another, stamped registration documents and a red chop
in the foreground; convert-hero scored 1406.6 full frame and 1358.7 crop). No
regeneration was needed and none was done.

## Locale work, in required order

Propagated to `blog-fr`, `blog-zh`, `blog-de` and `blog-es`, then ran
`deep-translate`, all three passes, FR then ZH then DE then ES. The native
in-language rewrite was run on every locale; none stopped at the humanized
translation. SEO fields were written in-language in each pass rather than
carried over from English.

Conventions carried from the 2026-09-21 publish rather than re-litigated:
FR ships regular spaces, not U+00A0, because all nineteen published FR articles
contain zero of them; ZH uses full-width double quotes “ ”; ES keeps the
*usted* register and "Agendar una llamada".

Terminology fixed per locale and used consistently:

| Concept | FR | ZH | DE | ES |
|---|---|---|---|---|
| 营业执照 | licence d'exploitation | 营业执照 | Gewerbelizenz | licencia de actividad |
| 商标注册证 | certificat de marque | 商标注册证 | Markenurkunde | certificado de marca |
| 授权书 | lettre d'autorisation | 授权书 | Vollmacht | carta de autorización |
| Authorized agency | agent agréé | 授权代理商 | autorisierte Agentur | agencia autorizada |
| 审核订单 | ordre d'examen | 审核订单 | Prüfauftrag | orden de revisión |

`Business Registration` and `Certificate of Incorporation` keep their English
names in every locale. They are the proper titles of specific documents, and
translating them would send a reader looking for the wrong paper.

`Blue V (蓝V)`, `Jiguang (聚光)`, `zhongcao (种草)` and `Alipay (支付宝)` follow
the existing per-locale renderings already in the corpus rather than new ones.

No banned word survived in any locale. The FR and ES drafts both reached for
*solutions* / *soluciones* once during the first pass and were rewritten.

## Listing surfaces and inbound links

- Every listing surface in this project is collection-driven. Verified by
  grepping the previously newest slug, `china-agency-pricing-models`, across
  `src/**/*.astro` and `src/**/*.ts`: zero hits outside the content
  collections. The five insights indexes, the five home previews,
  `InsightsGuides` and the Insights mega menu all read `getCollection` and sort
  by `publishDate`. Confirmed in the build output: all ten surfaces render the
  new article.
- The mega menu lists only the `industries` and `tools` collections plus CEO
  columns, so an insight does not appear there. Same as the previous article,
  nothing missing.
- Added to `public/llms.txt`, after the china-agency-pricing-models entry.
- Inbound links added in all five locales, three articles:
  1. `xiaohongshu-algorithm` already carried a plain-text forward reference to
     this guide in every locale, left there by the 2026-09-15 publish. Those
     five sentences became links. No new copy.
  2. `first-90-days-china-social-media`, a new paragraph after the Xiaohongshu
     verification blockquote, natively worded per locale.
  3. `xiaohongshu-marketing-foreign-brands`, appended to the closing
     related-reading paragraph.

The 03B asset brief asks for those in-links "EN only unless a locale pass is
approved separately". That was overridden, as it was on 2026-09-15, 2026-09-17
and 2026-09-21: an article's inbound link belongs in every locale that carries
the article, or the link graph diverges by language. The precedent is now four
runs deep. Logged because the single-locale default would otherwise have
scoped it to English.

`public/llms.txt` still carries the pre-existing stray blank line mid-list
around line 57. Left alone, unrelated to this publish.

## Verification

`npm run build` passed, server built in 26.87s. `npx astro check` passed:
287 files, 0 errors, 0 warnings, 172 hints.

All five locale pages were emitted and all five appear in `sitemap-0.xml` with
the complete hreflang alternate set, including `x-default`.

Every locale carries 10 H2 sections, 3 FAQ questions, 3 tables and 16
blockquote citations, and the set of 8 distinct source URLs is identical across
all five files, with nothing missing and nothing extra. Each has five internal
links on locale-correct routes. No em or en dash, no exclamation mark, no
U+00A0, no banned word, no editorial marker or HTML comment, no duplicate H1.
ZH carries zero half-width Latin punctuation after a Chinese character.

| Locale | SEO title characters | Meta description characters |
|---|---:|---:|
| EN | 40 | 140 |
| FR | 45 | 140 |
| ZH | 10 | 50 |
| DE | 40 | 130 |
| ES | 38 | 144 |

House ceilings are 52 and 152. No locale needed trimming this time.

## Open items

- `TODO: client sign-off` remains open, carried from the draft. The brief asked
  for rejection reasons drawn from our own submission history, labeled as ours.
  No such entry exists in `sources/verified-sources.md`, nothing was estimated,
  and the five-reason section stands entirely on grounds the platform
  publishes. To add the first-party version later, the ledger needs:
  applications submitted, period, count by rejection reason, exclusions and
  written clearance. The article does not depend on it.
- The asset brief asks for five to seven numbered screenshots of the real
  application flow. The pipeline cannot produce screenshots. The brief block
  names the exact seven screens in order on the desktop site, plus a redaction
  list, and that substitution is already logged in `logs/2026-09-18.md`. Still
  outstanding as a design task.
- The slug is English under all four non-English locales, which is how every
  article in this collection routes. Flagged, not renamed.

## Model

Opus, attempt 1 of the 2026-09-22 publish fallback chain. Every step ran on the
most capable model available. No step was downgraded.
