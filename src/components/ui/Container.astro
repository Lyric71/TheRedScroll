---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['max-w-[1200px] mx-auto px-[48px] max-[1024px]:px-[32px] max-[640px]:px-[20px]', className]}>
  <slot />
</div>
