---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['w-[40px] h-[3px] bg-[var(--color-vermillion)] rounded-none mb-[12px]', className]}></div>
