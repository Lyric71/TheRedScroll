---
import PageLayout from '../../../layouts/PageLayout.astro';
import Container from '../../../components/ui/Container.astro';
import SectionBar from '../../../components/ui/SectionBar.astro';
import SectionHeading from '../../../components/ui/SectionHeading.astro';
import Button from '../../../components/ui/Button.astro';

const platforms = [
  {
    name: 'Bilibili',
    chineseName: 'B站',
    mau: '366M',
    audience: 'Gen Z、城市用户、受过高等教育。78%在35岁以下',
    category: '视频与社区',
    description: '国内长视频的标杆。起家于二次元和游戏，现在科技、教育、美妆、汽车等等品类都覆盖了。接近70%的国内Gen Z是活跃用户。他们不是快速划视频的那种——点进去就是坐下来看。人均单次使用时长：107分钟。',
    detail: 'B站在2025年第一次实现盈利。广告收入同比增长27%。弹幕（在视频上方滚动的实时评论）让被动观看变成了近乎集体的体验。',
    bestFor: '科技、游戏、教育、美妆、汽车。主要格式：深度解析、教程、开箱系列、跟UP主联合制作。',
    image: '/images/platforms/additional-platforms/bilibili.png',
    logo: '/images/platforms/bilibili-logo.svg',
    brandColor: '#00A1D6',
    bg: '#F7F2EE',
    iconBg: '#E8E0D8',
    textColor: '#1A1A1A',
    descColor: '#5C5C5C',
    linkColor: '#CC3333',
  },
  {
    name: 'Kuaishou',
    chineseName: '快手',
    mau: '731M',
    audience: '覆盖广，三四线城市存在感特别强',
    category: '视频与社区',
    description: '月活7.31亿。用户主要分布在三到五线城市，更吃真实感，不吃过度精致的品牌视频。"老铁"文化奖励那些持续更新、说话不拐弯的创作者。',
    detail: '一个大多数品牌忽略的细节：40岁以上用户占了平台电商总支出的47%以上。这是一个大多数企业根本没考虑过的人群。',
    bestFor: '快消、农产品、面向一二线以外市场的品牌。主要格式：直播带货、产品演示、工厂探秘。',
    image: '/images/platforms/additional-platforms/kuaishou.png',
    logo: '/images/platforms/kuaishou-logo.svg',
    brandColor: '#FF4906',
    bg: '#1A1A1A',
    iconBg: 'rgba(255,255,255,0.1)',
    textColor: '#FFFFFF',
    descColor: 'rgba(255,255,255,0.6)',
    linkColor: '#CC3333',
  },
  {
    name: 'Zhihu',
    chineseName: '知乎',
    mau: '~83M',
    audience: '专业人群，80%以上本科以上学历',
    category: '文字与知识',
    description: '国内最头部的问答平台。80%以上用户是本科及以上学历。2025年Q2女性用户占59%。内容被百度索引，能持续几个月带流量。抖音上一条内容48小时就消失了，知乎上的内容会持续复利。',
    detail: '月活大概8300万。用户规模不大，但都是"做过功课才下单"的高意向买家。其中1420万人还会为付费内容买单。',
    bestFor: 'B2B、专业服务、教育、科技、健康、奢侈品。主要格式：专家回答、行业分析、案例拆解。',
    image: '/images/platforms/additional-platforms/zhihu.png',
    logo: '/images/platforms/zhihu-logo.svg',
    brandColor: '#0066FF',
    bg: '#CC3333',
    iconBg: 'rgba(255,255,255,0.2)',
    textColor: '#FFFFFF',
    descColor: 'rgba(255,255,255,0.8)',
    linkColor: '#FFFFFF',
  },
  {
    name: 'Toutiao',
    chineseName: '今日头条',
    mau: '410M',
    audience: '新闻阅读者，25到35岁',
    category: '文字与知识',
    description: '字节跳动的新闻平台。月活4.1亿。内容按兴趣分发，不看粉丝数。一个零粉丝的品牌账号，如果文章跑起来了，第一天就能触达几百万人。',
    detail: '开户流程比较折腾，封号也比较快。需要懂规矩的团队来操作。',
    bestFor: 'B2B、工业、科技、新品发布。',
    image: '/images/platforms/additional-platforms/toutiao.png',
    logo: '/images/platforms/toutiao-logo.svg',
    brandColor: '#ED1C24',
    bg: '#E8E0D8',
    iconBg: '#FFFFFF',
    textColor: '#1A1A1A',
    descColor: '#5C5C5C',
    linkColor: '#CC3333',
  },
  {
    name: 'Baidu Tieba',
    chineseName: '百度贴吧',
    mau: '300M',
    audience: '按兴趣聚合的话题社区',
    category: '文字与知识',
    description: '国内最早的论坛，按关键词组织，有点像Reddit。月活3亿。所有帖子都被百度索引——活跃参与既能在社区里建立信任，也能拉SEO。',
    detail: '',
    bestFor: '消费电子、游戏、汽车、以及有垂直社区的品牌。',
    image: '/images/platforms/additional-platforms/baidu-tieba.png',
    logo: '/images/platforms/baidu-tieba-logo.svg',
    brandColor: '#4879BD',
    bg: '#F7F2EE',
    iconBg: '#E8E0D8',
    textColor: '#1A1A1A',
    descColor: '#5C5C5C',
    linkColor: '#CC3333',
  },
  {
    name: 'Meituan/Dianping',
    chineseName: '美团/大众点评',
    mau: '770M+',
    audience: '餐饮、出行、本地生活',
    category: '电商与生活',
    description: '国内最大的本地生活服务平台。年活跃用户超过7.7亿，商家1450万。下单之前，消费者要来大众点评翻一圈评价。一个精心维护的商家页能带来到店流量，一个没人管的页面直接把客户推给竞争对手。',
    detail: '',
    bestFor: '酒旅、餐饮、美业、养生、旅游、实体门店。',
    image: '/images/platforms/additional-platforms/meituan.png',
    logo: '/images/platforms/meituan-logo.svg',
    brandColor: '#FFD100',
    bg: '#1A1A1A',
    iconBg: 'rgba(255,255,255,0.1)',
    textColor: '#FFFFFF',
    descColor: 'rgba(255,255,255,0.6)',
    linkColor: '#CC3333',
  },
  {
    name: 'Pinduoduo',
    chineseName: '拼多多',
    mau: '880M+',
    audience: '价格敏感型买家，主要在三四线城市',
    category: '电商与生活',
    description: '国内第三大电商平台，母公司同时运营Temu。月活8.8亿以上，主要分布在三四线城市。核心玩法是拼团和深度折扣。平台还提供跨境电商和直播带货。',
    detail: '',
    bestFor: '快消、日用品、健康美妆、跨境。不适合奢侈品定位的品牌。',
    image: '/images/platforms/additional-platforms/pinduoduo.png',
    logo: '/images/platforms/pinduoduo-logo.svg',
    brandColor: '#E02E24',
    bg: '#B02D2D',
    iconBg: 'rgba(255,255,255,0.2)',
    textColor: '#FFFFFF',
    descColor: 'rgba(255,255,255,0.8)',
    linkColor: '#FFFFFF',
  },
  {
    name: 'Lemon8',
    chineseName: '柠檬8',
    mau: '23M',
    audience: '生活方式人群，看重视觉',
    category: '电商与生活',
    description: '字节跳动的生活方式视觉平台。2300万用户，还在持续涨。可以理解为Pinterest和小红书的结合体，围绕精致的审美内容搭建。平台还比较新，但涨得很快，尤其是美妆和旅游赛道。',
    detail: '',
    bestFor: '美妆、时尚、美食、旅游、家居。图片内容为主，轮播图、产品场景化拍摄。',
    image: '/images/platforms/additional-platforms/lemon8.png',
    logo: '/images/platforms/lemon8-logo.svg',
    brandColor: '#FFE135',
    bg: '#E8E0D8',
    iconBg: '#FFFFFF',
    textColor: '#1A1A1A',
    descColor: '#5C5C5C',
    linkColor: '#CC3333',
  },
  {
    name: 'QQ',
    chineseName: '腾讯QQ',
    mau: '534M',
    audience: '91%用户在15到24岁之间',
    category: '电商与生活',
    description: '月活5.34亿。91%的15到24岁用户都在上面。QQ群对国内最年轻的Gen Z来说有点像Discord服务器。平台还集成了游戏和音乐功能，学生群体的粘性非常高。',
    detail: 'QQ不是微信。完全不同的用户，完全不同的规则。',
    bestFor: '游戏、教育、娱乐、年轻化品牌。',
    image: '/images/platforms/additional-platforms/qq.png',
    logo: '/images/platforms/qq-logo.svg',
    brandColor: '#12B7F5',
    bg: '#F7F2EE',
    iconBg: '#E8E0D8',
    textColor: '#1A1A1A',
    descColor: '#5C5C5C',
    linkColor: '#CC3333',
  },
];

const stats = [
  { label: 'B站人均日使用', value: '107 min', image: '/images/platforms/additional-platforms/bilibili.png' },
  { label: '快手月活', value: '731M', image: '/images/platforms/additional-platforms/kuaishou.png' },
  { label: '拼多多月活', value: '880M+', image: '/images/platforms/additional-platforms/pinduoduo.png' },
  { label: '美团年活跃用户', value: '770M+', image: '/images/platforms/additional-platforms/meituan.png' },
  { label: 'QQ月活', value: '534M', image: '/images/platforms/additional-platforms/qq.png' },
  { label: '今日头条月活', value: '410M', image: '/images/platforms/additional-platforms/toutiao.png' },
  { label: 'B站月活', value: '366M', image: '/images/platforms/additional-platforms/bilibili.png' },
  { label: '百度贴吧月活', value: '300M', image: '/images/platforms/additional-platforms/baidu-tieba.png' },
  { label: '知乎月活', value: '~83M', image: '/images/platforms/additional-platforms/zhihu.png' },
  { label: 'Lemon8用户', value: '23M', image: '/images/platforms/additional-platforms/lemon8.png' },
];

const services = [
  {
    title: '开户和认证',
    description: '注册、认证、主页优化。每个平台的合规流程我们都跑一遍。所有的中文表单和行政流程我们接，你不用卡在那里。',
    result: '账号已认证，可以立刻开始发布。',
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 12l2 2 4-4"/><path d="M12 2a10 10 0 1 0 0 20 10 10 0 0 0 0-20z"/></svg>`,
    image: '/images/platforms/additional-services/account-setup.png',
  },
  {
    title: '内容制作',
    description: 'HubStudio.ai做AI视觉，本地中文写手写文案。策略始终由人拿。每一条内容在发布之前都由本地写手过一遍：语气、文化、平台规则。每个平台的格式和用户调性都会单独适配。',
    result: '用户真的愿意点进来看的本土内容。',
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/><polyline points="14,2 14,8 20,8"/></svg>`,
    image: '/images/platforms/additional-services/content-production.png',
  },
  {
    title: '发布和社群运营',
    description: '按平台的最佳时段定时发布、评论维护、社群互动、涨粉。发布频率在合同范围里提前写清楚。',
    result: '稳定的存在感，加上活跃的社群互动。',
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>`,
    image: '/images/platforms/additional-services/publishing.png',
  },
  {
    title: '广告投放',
    description: '开广告账户、跑campaign、出素材、做效果报告。广告预算按实际成本走账，不加点。',
    result: '精准触达目标人群，钱花到哪儿都清楚。',
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>`,
    image: '/images/platforms/additional-services/advertising.png',
  },
  {
    title: 'KOL与KOC项目',
    description: '跨平台选人、核数据、跑项目、出ROI报告。B站的UP主、快手的达人、知乎的大V——选人、核、执行、复盘。只用真实可核的互动数据。',
    result: '有透明ROI数据的达人项目。',
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>`,
    image: '/images/platforms/additional-services/kol-campaigns.png',
  },
  {
    title: '数据和报告',
    description: '每月报告覆盖涨粉、互动、内容效果，以及跟你业务相关的KPI。所有报告用英文给，中文原始数据附上方便你本地团队对。',
    result: '一份能直接指导下个月的清楚报告。',
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><path d="M7 16l4-8 4 4 6-8"/></svg>`,
    image: '/images/platforms/additional-services/analytics.png',
  },
];

const recommendations = [
  { goal: '用长视频触达Gen Z', platforms: 'B站' },
  { goal: '中小城市的价格敏感人群', platforms: '快手、拼多多' },
  { goal: '搜索长尾流量', platforms: '知乎、百度贴吧' },
  { goal: '线下门店导流', platforms: '美团/大众点评' },
  { goal: '新闻类大众传播', platforms: '今日头条' },
  { goal: '学生和游戏玩家', platforms: 'QQ' },
  { goal: '生活方式视觉存在感', platforms: 'Lemon8' },
];

const steps = [
  {
    number: '01',
    title: '先聊聊。',
    description: '30分钟。你的品牌、用户、在中国的目标。不跑销售流程，就问该问的问题。',
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#CC3333" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>`,
  },
  {
    number: '02',
    title: '平台推荐。',
    description: '我们帮你挑适合的平台，打包进你的方案里。范围清楚，一口价。',
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#CC3333" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/><polyline points="14,2 14,8 20,8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>`,
  },
  {
    number: '03',
    title: '启动。',
    description: '开户、首批内容、持续运营。一般两周内。',
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#CC3333" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>`,
  },
];

const faqs = [
  {
    q: '这些平台的内容是谁做的？',
    a: '本地中文写手，人在上海和香港。HubStudio.ai提速视觉制作，但内容策略是人在做，每一条都由本地写手复核文化、tone和合规。没有任何一条内容是没过人就发出去的。',
  },
  {
    q: '你们真的能访问这14个平台吗？',
    a: '能。我们每天都从国内登录这些平台操作。有几个平台对海外IP有功能限制。在海外远程操作的代理经常碰到访问问题——他们解决不了。我们没有这个问题。',
  },
  {
    q: '怎么帮我们选对的平台？',
    a: '按你的行业、用户、目标来分析。做酒旅的必须走美团/大众点评。B2B科技类在知乎和今日头条上效果更好。我们推荐的是真正适合你业务的，不是为了堆满方案显得服务多。',
  },
  {
    q: '已经签了合同，还能加平台吗？',
    a: '能。任何平台、任何时候都能加。我们把它加进你的范围里，交付物和价格都写清楚。',
  },
  {
    q: '"AI辅助内容"具体是什么意思？',
    a: '策略和方向100%是人做的。HubStudio.ai负责视觉这一端——产品图、社媒素材、视频草稿。然后本地写手过每一条，核准确度、平台规则、品牌调性。AI提速，人保证对不对。',
  },
  {
    q: '各平台的内容审核规则你们怎么处理？',
    a: '每个平台一套审核系统，差别很大。B站会标的关键词，知乎根本不在意。今日头条封号比大多数平台都快。什么能发、什么会被封，我们每天都在处理，心里有数。',
  },
  {
    q: '开一个新平台要多久？',
    a: '看平台，一到三周。注册、认证、主页配置、首批内容我们都接。大多数客户两到三周内能看到首条内容上线。',
  },
  {
    q: '这些平台上你们做达人项目吗？',
    a: '做。B站UP主、快手达人、知乎大V都做。选人、核数据、执行、复盘。达人费用单独走账，完全透明。',
  },
  {
    q: '怎么衡量效果？',
    a: '数据直接从平台后台拉，不是从第三方工具。报告覆盖涨粉、互动、内容效果和每个平台各自的KPI。知乎上看的是百度搜索结果里的展现量。美团上看的是评论量和评分变化。快手上看直播带货转化。我们汇报的是真正能推进你业务的那些数据。',
  },
  {
    q: '价格怎么算？',
    a: '范围写死，一口价。每一项交付物在启动前就定好。每个平台一份月度固定费用。没有时薪，也没有范围蔓延的额外账单。广告预算永远单独结算，按实际成本走。',
  },
];

const videoAndCommunity = platforms.filter(p => p.category === '视频与社区');
const textAndKnowledge = platforms.filter(p => p.category === '文字与知识');
const commerceAndLifestyle = platforms.filter(p => p.category === '电商与生活');
---

<PageLayout
  title="Plateformes complementaires : Bilibili, Kuaishou, Zhihu et plus | TheRedScroll"
  description="14 plateformes que la plupart des agences ignorent. Bilibili, Kuaishou, Zhihu, Toutiao, Meituan, Pinduoduo et d'autres. Perimetre fixe, prix fixe. Equipe sur le terrain en Chine."
  lang="zh"
>
  <!-- Hero: Dark hero banner -->
  <section class="w-full bg-[#1A1A1A] py-[80px] max-[640px]:py-[48px] relative overflow-hidden">
    <div class="absolute inset-0 bg-cover bg-center opacity-[0.2]" style="background-image: url('/images/hero/additional-platforms-hero.png')" aria-hidden="true"></div>
    <span class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 text-[140px] max-[640px]:text-[80px] font-medium text-[#FFFFFF] opacity-[0.03] pointer-events-none select-none whitespace-nowrap" aria-hidden="true">
      更多平台
    </span>
    <Container class="relative z-[1]">
      <div class="max-w-[800px] mx-auto text-center">
        <p class="text-[11px] font-medium uppercase tracking-[0.05em] leading-[1.2] text-[#CC3333] mb-[8px]">
          Au-dela des quatre grandes
        </p>
        <h1 class="text-[40px] max-[640px]:text-[28px] font-medium leading-[1.2] text-[#FFFFFF] mb-[8px]">
          14 plateformes que la plupart des agences ignorent
        </h1>
        <p class="text-[14px] text-[rgba(255,255,255,0.6)] font-normal mb-[24px]">
          Vos clients regardent Bilibili pendant deux heures d'affilee. Ils consultent Dianping avant de choisir un restaurant. Ils font davantage confiance aux reponses sur Zhihu qu'a la publicite. Ils achetent en live sur Kuaishou dans le metro. Nous publions sur toutes ces plateformes. Chaque jour, depuis Shanghai et Hong Kong.
        </p>
        <div class="flex items-center justify-center gap-[12px] flex-wrap">
          <Button variant="primary" href="/zh/contact">Prendre rendez-vous</Button>
        </div>
      </div>
    </Container>
  </section>

  <!-- Stats section — horizontal scroll strip -->
  <section class="mt-[96px] max-[640px]:mt-[64px]">
    <Container>
      <div class="flex items-end justify-between gap-[32px] max-[640px]:flex-col max-[640px]:items-start max-[640px]:gap-[16px] mb-[32px]">
        <div class="max-w-[560px]">
          <SectionBar />
          <h2 class="text-[28px] max-[640px]:text-[22px] font-medium leading-[1.25] text-[#1A1A1A] mb-[8px]">
            Plus de 1,1 milliard de Chinois utilisent les reseaux sociaux. Ils ne se limitent pas a quatre applications.
          </h2>
          <p class="text-[14px] text-[#5C5C5C] font-normal leading-[1.6]">
            Les chiffres cles sur 9 plateformes supplementaires.
          </p>
        </div>
        <Button variant="primary" href="/zh/contact" class="shrink-0">Prendre rendez-vous</Button>
      </div>
    </Container>

    <div class="relative">
      <div class="absolute right-0 top-0 bottom-0 w-[80px] bg-gradient-to-l from-[#FFFFFF] to-transparent z-[1] pointer-events-none" aria-hidden="true"></div>

      <div class="overflow-x-auto scrollbar-hide" id="stats-scroll">
        <div class="flex gap-[16px] px-[48px] max-[1024px]:px-[32px] max-[640px]:px-[20px] pb-[8px]" style="min-width: min-content;">
          {stats.map((stat, i) => (
            <div
              class="stat-card relative shrink-0 w-[220px] max-[640px]:w-[180px] h-[200px] max-[640px]:h-[170px] rounded-[12px] overflow-hidden flex flex-col justify-end cursor-grab select-none"
              data-stat-index={i}
            >
              <div class="absolute inset-0 bg-cover bg-center transition-transform duration-500 ease-out stat-bg" style={`background-image: url('${stat.image}')`} aria-hidden="true"></div>
              <div class="absolute inset-0 bg-gradient-to-t from-[rgba(26,26,26,0.9)] via-[rgba(26,26,26,0.4)] to-transparent" aria-hidden="true"></div>
              <div class="relative z-[1] p-[20px]">
                <span class="text-[32px] text-[#FFFFFF] font-medium leading-none stat-counter block mb-[4px]" data-target={stat.value}>0</span>
                <span class="text-[12px] text-[rgba(255,255,255,0.6)] font-normal leading-[1.4]">{stat.label}</span>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Why these platforms matter -->
  <section class="mt-[96px] max-[640px]:mt-[64px]">
    <Container>
      <div class="text-center mb-[48px] max-[640px]:mb-[32px]">
        <div class="flex justify-center mb-[12px]">
          <SectionBar />
        </div>
        <SectionHeading overline="Pourquoi ces plateformes comptent">
          La plupart des agences s'arretent a quatre. Aller plus loin exige une equipe sur le terrain.
        </SectionHeading>
      </div>

      <div class="grid grid-cols-3 max-[1024px]:grid-cols-2 max-[640px]:grid-cols-1 gap-[16px]">
        <div class="bg-[#F7F2EE] border-none rounded-[10px] overflow-hidden">
          <div class="w-full aspect-[16/9] overflow-hidden">
            <img src="/images/platforms/additional-why/focused-attention.png" alt="Personne regardant du contenu video long format avec une attention soutenue" class="w-full h-full object-cover" loading="lazy" decoding="async" />
          </div>
          <div class="p-[24px] max-[640px]:p-[20px]">
            <h3 class="text-[18px] font-medium leading-[1.3] text-[#1A1A1A] mb-[8px]">Une attention soutenue, pas du scroll rapide</h3>
            <p class="text-[13px] text-[#5C5C5C] font-normal leading-[1.5]">
              Les utilisateurs de Bilibili passent 107 minutes par jour sur la plateforme. Pres de deux heures d'attention soutenue de la part de consommateurs diplomes de moins de 35 ans. Ce n'est pas du scroll rapide. C'est une audience captive.
            </p>
          </div>
        </div>

        <div class="bg-[#F7F2EE] border-none rounded-[10px] overflow-hidden">
          <div class="w-full aspect-[16/9] overflow-hidden">
            <img src="/images/platforms/additional-why/ecommerce-power.png" alt="Colis e-commerce en cours de tri dans un centre logistique chinois" class="w-full h-full object-cover" loading="lazy" decoding="async" />
          </div>
          <div class="p-[24px] max-[640px]:p-[20px]">
            <h3 class="text-[18px] font-medium leading-[1.3] text-[#1A1A1A] mb-[8px]">Du commerce reel, des volumes reels</h3>
            <p class="text-[13px] text-[#5C5C5C] font-normal leading-[1.5]">
              Kuaishou a traite 385 milliards de RMB de ventes e-commerce au T3 2025. En un seul trimestre. Ces plateformes deplacent des volumes de produits a une echelle que la plupart des marques n'ont meme pas envisagee.
            </p>
          </div>
        </div>

        <div class="bg-[#F7F2EE] border-none rounded-[10px] overflow-hidden">
          <div class="w-full aspect-[16/9] overflow-hidden">
            <img src="/images/platforms/additional-why/search-longevity.png" alt="Reponses Zhihu classees dans les resultats de recherche Baidu" class="w-full h-full object-cover" loading="lazy" decoding="async" />
          </div>
          <div class="p-[24px] max-[640px]:p-[20px]">
            <h3 class="text-[18px] font-medium leading-[1.3] text-[#1A1A1A] mb-[8px]">Du contenu qui se capitalise</h3>
            <p class="text-[13px] text-[#5C5C5C] font-normal leading-[1.5]">
              Les reponses Zhihu se classent dans les resultats de recherche Baidu pendant des mois. Parfois des annees. Sur Douyin, une publication disparait en 48 heures. Sur Zhihu, elle continue de generer du trafic bien apres sa mise en ligne.
            </p>
          </div>
        </div>
      </div>

      <p class="text-[14px] text-[#5C5C5C] font-normal leading-[1.5] mt-[24px] text-center max-w-[680px] mx-auto">
        Aller au-dela de quatre plateformes necessite des locuteurs natifs, un acces quotidien aux plateformes et une equipe qui opere sur le marche a temps plein. C'est exactement notre cas.
      </p>
    </Container>
  </section>

  <!-- Video and community platforms -->
  <section class="mt-[96px] max-[640px]:mt-[64px]">
    <Container>
      <div class="text-center mb-[48px] max-[640px]:mb-[32px]">
        <div class="flex justify-center mb-[12px]">
          <SectionBar />
        </div>
        <SectionHeading overline="Video et communaute">
          Video longue, live commerce et audiences fideles
        </SectionHeading>
      </div>

      <div class="grid grid-cols-2 max-[640px]:grid-cols-1 gap-[16px]">
        {videoAndCommunity.map((item) => (
          <div
            class="eco-card relative border-none overflow-hidden flex flex-col justify-between"
            style={`border-radius: ${item.bg === '#1A1A1A' || item.bg === '#CC3333' || item.bg === '#B02D2D' ? '12px' : '10px'}`}
            data-tilt-eco
          >
            <div class="absolute inset-0 bg-cover bg-center transition-transform duration-500 ease-out eco-bg" style={`background-image: url('${item.image}')`} aria-hidden="true"></div>
            <div class="absolute inset-0" style={`background: ${item.bg}; opacity: 0.72`} aria-hidden="true"></div>
            <div class="relative z-[1] p-[24px] max-[640px]:p-[20px]">
              <div class="flex items-center gap-[12px] mb-[16px]">
                <div class="w-[40px] h-[40px] rounded-[6px] flex items-center justify-center" style={`background: ${item.iconBg}`}>
                  <img src={item.logo} alt={`Logo ${item.name}`} width="20" height="20" style={item.textColor === '#FFFFFF' ? 'filter: brightness(0) invert(1);' : ''} />
                </div>
                <div>
                  <h3 class="text-[18px] font-medium leading-[1.3]" style={`color: ${item.textColor}`}>
                    {item.name}
                  </h3>
                  <span class="text-[12px] font-normal block" style={`color: ${item.descColor}`}>{item.chineseName} · {item.mau} MAU</span>
                </div>
              </div>
              <p class="text-[13px] font-normal leading-[1.5] mb-[12px]" style={`color: ${item.descColor}`}>
                {item.description}
              </p>
              {item.detail && (
                <p class="text-[13px] font-normal leading-[1.5] mb-[12px]" style={`color: ${item.descColor}`}>
                  {item.detail}
                </p>
              )}
              <p class="text-[12px] font-medium leading-[1.5]" style={`color: ${item.linkColor}`}>
                Ideal pour : {item.bestFor}
              </p>
            </div>
          </div>
        ))}
      </div>
    </Container>
  </section>

  <!-- Text and knowledge platforms -->
  <section class="mt-[96px] max-[640px]:mt-[64px]">
    <Container>
      <div class="text-center mb-[48px] max-[640px]:mb-[32px]">
        <div class="flex justify-center mb-[12px]">
          <SectionBar />
        </div>
        <SectionHeading overline="Texte et savoir">
          Questions-reponses, actualites, forums. Du contenu qui se reference et se capitalise.
        </SectionHeading>
      </div>

      <div class="grid grid-cols-3 max-[1024px]:grid-cols-2 max-[640px]:grid-cols-1 gap-[16px]">
        {textAndKnowledge.map((item) => (
          <div
            class="eco-card relative border-none overflow-hidden flex flex-col justify-between"
            style={`border-radius: ${item.bg === '#1A1A1A' || item.bg === '#CC3333' || item.bg === '#B02D2D' ? '12px' : '10px'}`}
            data-tilt-eco
          >
            <div class="absolute inset-0 bg-cover bg-center transition-transform duration-500 ease-out eco-bg" style={`background-image: url('${item.image}')`} aria-hidden="true"></div>
            <div class="absolute inset-0" style={`background: ${item.bg}; opacity: 0.72`} aria-hidden="true"></div>
            <div class="relative z-[1] p-[24px] max-[640px]:p-[20px]">
              <div class="flex items-center gap-[12px] mb-[16px]">
                <div class="w-[40px] h-[40px] rounded-[6px] flex items-center justify-center" style={`background: ${item.iconBg}`}>
                  <img src={item.logo} alt={`Logo ${item.name}`} width="20" height="20" style={item.textColor === '#FFFFFF' ? 'filter: brightness(0) invert(1);' : ''} />
                </div>
                <div>
                  <h3 class="text-[18px] font-medium leading-[1.3]" style={`color: ${item.textColor}`}>
                    {item.name}
                  </h3>
                  <span class="text-[12px] font-normal block" style={`color: ${item.descColor}`}>{item.chineseName} · {item.mau} MAU</span>
                </div>
              </div>
              <p class="text-[13px] font-normal leading-[1.5] mb-[12px]" style={`color: ${item.descColor}`}>
                {item.description}
              </p>
              {item.detail && (
                <p class="text-[13px] font-normal leading-[1.5] mb-[12px]" style={`color: ${item.descColor}`}>
                  {item.detail}
                </p>
              )}
              <p class="text-[12px] font-medium leading-[1.5]" style={`color: ${item.linkColor}`}>
                Ideal pour : {item.bestFor}
              </p>
            </div>
          </div>
        ))}
      </div>
    </Container>
  </section>

  <!-- Commerce and lifestyle platforms -->
  <section class="mt-[96px] max-[640px]:mt-[64px]">
    <Container>
      <div class="text-center mb-[48px] max-[640px]:mb-[32px]">
        <div class="flex justify-center mb-[12px]">
          <SectionBar />
        </div>
        <SectionHeading overline="Commerce et lifestyle">
          Avis, shopping, services locaux et contenu visuel
        </SectionHeading>
      </div>

      <div class="grid grid-cols-2 max-[1024px]:grid-cols-2 max-[640px]:grid-cols-1 gap-[16px]">
        {commerceAndLifestyle.map((item) => (
          <div
            class="eco-card relative border-none overflow-hidden flex flex-col justify-between"
            style={`border-radius: ${item.bg === '#1A1A1A' || item.bg === '#CC3333' || item.bg === '#B02D2D' ? '12px' : '10px'}`}
            data-tilt-eco
          >
            <div class="absolute inset-0 bg-cover bg-center transition-transform duration-500 ease-out eco-bg" style={`background-image: url('${item.image}')`} aria-hidden="true"></div>
            <div class="absolute inset-0" style={`background: ${item.bg}; opacity: 0.72`} aria-hidden="true"></div>
            <div class="relative z-[1] p-[24px] max-[640px]:p-[20px]">
              <div class="flex items-center gap-[12px] mb-[16px]">
                <div class="w-[40px] h-[40px] rounded-[6px] flex items-center justify-center" style={`background: ${item.iconBg}`}>
                  <img src={item.logo} alt={`Logo ${item.name}`} width="20" height="20" style={item.textColor === '#FFFFFF' ? 'filter: brightness(0) invert(1);' : ''} />
                </div>
                <div>
                  <h3 class="text-[18px] font-medium leading-[1.3]" style={`color: ${item.textColor}`}>
                    {item.name}
                  </h3>
                  <span class="text-[12px] font-normal block" style={`color: ${item.descColor}`}>{item.chineseName} · {item.mau} MAU</span>
                </div>
              </div>
              <p class="text-[13px] font-normal leading-[1.5] mb-[12px]" style={`color: ${item.descColor}`}>
                {item.description}
              </p>
              {item.detail && (
                <p class="text-[13px] font-normal leading-[1.5] mb-[12px]" style={`color: ${item.descColor}`}>
                  {item.detail}
                </p>
              )}
              <p class="text-[12px] font-medium leading-[1.5]" style={`color: ${item.linkColor}`}>
                Ideal pour : {item.bestFor}
              </p>
            </div>
          </div>
        ))}
      </div>
    </Container>
  </section>

  <!-- What we do — services -->
  <section class="mt-[96px] max-[640px]:mt-[64px]">
    <Container>
      <div class="text-center mb-[48px] max-[640px]:mb-[32px]">
        <div class="flex justify-center mb-[12px]">
          <SectionBar />
        </div>
        <SectionHeading overline="Nos services">
          Le meme modele sur chaque plateforme. Perimetre clair. Prix fixe.
        </SectionHeading>
      </div>

      <!-- Desktop: two-column selector + panel -->
      <div class="grid grid-cols-[340px_1fr] max-[1024px]:grid-cols-[280px_1fr] max-[768px]:hidden gap-[24px] items-stretch">
        <div class="flex flex-col gap-[4px]">
          {services.map((service, i) => (
            <button
              class:list={[
                'svc-selector flex items-center gap-[12px] px-[16px] py-[14px] rounded-[8px] text-left cursor-pointer border-none transition-all duration-300 relative',
                i === 0 ? 'svc-selector-active' : '',
              ]}
              data-svc-select={i}
              style={i === 0 ? 'background: #F7F2EE' : 'background: transparent'}
            >
              <div
                class="svc-sel-indicator absolute left-0 top-[12px] bottom-[12px] w-[3px] rounded-full transition-all duration-300"
                style={i === 0 ? 'background: #CC3333; opacity: 1' : 'background: #CC3333; opacity: 0'}
              ></div>
              <div
                class="svc-sel-icon w-[36px] h-[36px] rounded-[6px] shrink-0 flex items-center justify-center transition-all duration-300"
                style={i === 0 ? 'background: #E8E0D8; color: #CC3333' : 'background: transparent; color: #5C5C5C'}
              >
                <Fragment set:html={service.icon} />
              </div>
              <span
                class="svc-sel-label text-[14px] font-medium leading-[1.3] transition-colors duration-300"
                style={i === 0 ? 'color: #1A1A1A' : 'color: #5C5C5C'}
              >
                {service.title}
              </span>
            </button>
          ))}
        </div>

        <div class="relative">
          {services.map((service, i) => (
            <div
              class="svc-panel relative rounded-[12px] overflow-hidden h-full"
              data-svc-panel={i}
              style={i === 0 ? '' : 'display: none;'}
            >
              <div class="absolute inset-0 bg-cover bg-center" style={`background-image: url('${service.image}')`} aria-hidden="true"></div>
              <div class="absolute inset-0 bg-gradient-to-t from-[rgba(26,26,26,0.95)] via-[rgba(26,26,26,0.6)] to-[rgba(26,26,26,0.15)]" aria-hidden="true"></div>
              <div class="relative z-[1] p-[32px] max-[1024px]:p-[24px] h-full flex flex-col justify-end">
                <h3 class="text-[22px] font-medium leading-[1.25] mb-[8px] text-[#FFFFFF]">
                  {service.title}
                </h3>
                <p class="text-[14px] font-normal leading-[1.6] text-[rgba(255,255,255,0.7)] mb-[16px]">
                  {service.description}
                </p>
                <div class="flex items-center gap-[8px] pt-[12px] border-t border-[rgba(255,255,255,0.15)]">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#CC3333" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 12l2 2 4-4"/><circle cx="12" cy="12" r="10"/></svg>
                  <p class="text-[13px] font-medium leading-[1.5] text-[#FFFFFF]">
                    {service.result}
                  </p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Mobile: stacked cards -->
      <div class="hidden max-[768px]:flex flex-col gap-[12px]">
        {services.map((service) => (
          <div class="relative rounded-[12px] overflow-hidden">
            <div class="absolute inset-0 bg-cover bg-center" style={`background-image: url('${service.image}')`} aria-hidden="true"></div>
            <div class="absolute inset-0 bg-[#1A1A1A] opacity-[0.82]" aria-hidden="true"></div>
            <div class="relative z-[1] p-[24px]">
              <div class="w-[40px] h-[40px] rounded-[6px] flex items-center justify-center mb-[12px] bg-[rgba(255,255,255,0.12)] text-[#FFFFFF]">
                <Fragment set:html={service.icon} />
              </div>
              <h3 class="text-[18px] font-medium leading-[1.3] mb-[8px] text-[#FFFFFF]">
                {service.title}
              </h3>
              <p class="text-[13px] font-normal leading-[1.5] mb-[12px] text-[rgba(255,255,255,0.7)]">
                {service.description}
              </p>
              <p class="text-[13px] font-medium leading-[1.5] text-[#FFFFFF]">
                Resultat : {service.result}
              </p>
            </div>
          </div>
        ))}
      </div>
    </Container>
  </section>

  <!-- Which platforms fit — dark section -->
  <section class="mt-[96px] max-[640px]:mt-[64px]">
    <Container>
      <div class="bg-[#1A1A1A] rounded-[12px] py-[48px] px-[48px] max-[640px]:px-[20px] max-[640px]:py-[32px]">
        <div class="mb-[40px] max-[640px]:mb-[32px]">
          <p class="text-[11px] font-medium uppercase tracking-[0.05em] leading-[1.2] text-[#CC3333] mb-[8px]">
            Recommandations par plateforme
          </p>
          <h2 class="text-[28px] max-[640px]:text-[22px] font-medium leading-[1.25] text-[#FFFFFF]">
            Quelles plateformes pour votre marque ?
          </h2>
          <p class="text-[14px] text-[rgba(255,255,255,0.6)] font-normal leading-[1.5] mt-[8px] max-w-[680px]">
            Ces plateformes fonctionnent au mieux en complement de WeChat, Douyin, RedNote et Weibo. Nous vous aidons a construire le dispositif complet.
          </p>
        </div>

        <div class="grid grid-cols-2 max-[640px]:grid-cols-1 gap-[20px]">
          {recommendations.map((rec) => (
            <div class="border-l-[3px] border-[#CC3333] pl-[16px]">
              <h3 class="text-[16px] text-[#FFFFFF] font-medium leading-[1.3] mb-[4px]">
                {rec.goal}
              </h3>
              <p class="text-[14px] text-[rgba(255,255,255,0.6)] font-normal leading-[1.5]">
                {rec.platforms}
              </p>
            </div>
          ))}
        </div>
      </div>
    </Container>
  </section>

  <!-- FAQ -->
  <section class="mt-[96px] max-[640px]:mt-[64px]">
    <Container>
      <div class="grid grid-cols-[340px_1fr] max-[1024px]:grid-cols-[280px_1fr] max-[768px]:grid-cols-1 gap-[48px] max-[768px]:gap-[32px]">
        <div>
          <div class="sticky top-[84px] max-[640px]:static">
            <SectionBar />
            <p class="text-[11px] font-medium uppercase tracking-[0.05em] leading-[1.2] text-[#CC3333] mb-[8px]">FAQ</p>
            <h2 class="text-[28px] max-[640px]:text-[22px] font-medium leading-[1.25] text-[#1A1A1A] mb-[16px]">
              Questions frequentes
            </h2>
            <p class="text-[14px] text-[#5C5C5C] font-normal leading-[1.6]">
              Tout ce qu'il faut savoir pour travailler avec TheRedScroll sur ces plateformes.
            </p>
          </div>
        </div>

        <div>
          {faqs.map((faq) => (
            <div class="border-b border-[rgba(0,0,0,0.08)]" data-faq>
              <button
                class="w-full flex items-center justify-between py-[20px] text-left bg-transparent border-none cursor-pointer group"
                data-faq-trigger
                aria-expanded="false"
              >
                <h3 class="text-[16px] font-medium leading-[1.3] text-[#1A1A1A] group-hover:text-[#5C5C5C] transition-colors duration-150 pr-[16px]">{faq.q}</h3>
                <svg class="w-[16px] h-[16px] shrink-0 text-[#5C5C5C] transition-transform duration-300 faq-chevron" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M4 6l4 4 4-4"/>
                </svg>
              </button>
              <div class="faq-content overflow-hidden" style="height: 0; opacity: 0;">
                <div class="pb-[20px]">
                  <p class="text-[14px] text-[#5C5C5C] font-normal leading-[1.6]">
                    {faq.a}
                  </p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </Container>
  </section>

  <!-- Getting started -->
  <section class="relative mt-[96px] max-[640px]:mt-[64px] pt-[80px] pb-[80px] max-[640px]:pt-[48px] max-[640px]:pb-[48px] overflow-hidden">
    <div class="absolute inset-0 bg-cover bg-center" style="background-image: url('/images/hero/additional-platforms-hero.png')" aria-hidden="true"></div>
    <div class="absolute inset-0 bg-[#1A1A1A] opacity-[0.88]" aria-hidden="true"></div>

    <Container class="relative z-[1]">
      <div class="text-center mb-[48px] max-[640px]:mb-[32px]">
        <div class="flex justify-center mb-[12px]">
          <div class="w-[40px] h-[3px] bg-[#CC3333] rounded-none"></div>
        </div>
        <p class="text-[11px] font-medium uppercase tracking-[0.05em] leading-[1.2] text-[#CC3333] mb-[8px]">
          Pour commencer
        </p>
        <h2 class="text-[28px] max-[640px]:text-[22px] font-medium leading-[1.25] text-[#FFFFFF]">
          De zero a operationnel sur n'importe quelle plateforme en deux semaines.
        </h2>
      </div>

      <div class="grid grid-cols-3 max-[640px]:grid-cols-1 gap-[24px] max-[640px]:gap-[16px]">
        {steps.map((step) => (
          <div class="bg-[rgba(255,255,255,0.06)] border-[0.5px] border-[rgba(255,255,255,0.1)] rounded-[12px] p-[32px] max-[640px]:p-[20px] text-center">
            <div class="w-[64px] h-[64px] rounded-full bg-[rgba(255,255,255,0.08)] border-[0.5px] border-[rgba(255,255,255,0.15)] flex items-center justify-center mx-auto mb-[20px]">
              <Fragment set:html={step.icon} />
            </div>
            <h3 class="text-[18px] text-[#FFFFFF] font-medium leading-[1.3] mb-[8px]">
              {step.title}
            </h3>
            <p class="text-[14px] text-[rgba(255,255,255,0.6)] font-normal leading-[1.6]">
              {step.description}
            </p>
          </div>
        ))}
      </div>

      <div class="text-center mt-[40px]">
        <Button variant="primary" href="/zh/contact">Prendre rendez-vous</Button>
      </div>

      <!-- Cross-links -->
      <div class="mt-[48px] pt-[24px] border-t border-[rgba(255,255,255,0.1)] text-center">
        <p class="text-[13px] text-[rgba(255,255,255,0.4)] font-normal mb-[12px]">Voir aussi</p>
        <div class="flex justify-center gap-[24px] flex-wrap">
          <a href="/zh/platforms/wechat" class="text-[14px] text-[#CC3333] font-medium transition-colors duration-150 hover:text-[#B02D2D]">Marketing WeChat &rarr;</a>
          <a href="/zh/platforms/douyin" class="text-[14px] text-[#CC3333] font-medium transition-colors duration-150 hover:text-[#B02D2D]">Marketing Douyin &rarr;</a>
          <a href="/zh/platforms/rednote" class="text-[14px] text-[#CC3333] font-medium transition-colors duration-150 hover:text-[#B02D2D]">Marketing RedNote &rarr;</a>
          <a href="/zh/platforms/weibo" class="text-[14px] text-[#CC3333] font-medium transition-colors duration-150 hover:text-[#B02D2D]">Marketing Weibo &rarr;</a>
        </div>
      </div>
    </Container>
  </section>

<style>
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
  .svc-panel {
    transition: opacity 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .svc-selector:hover {
    background: #F7F2EE !important;
  }
  .faq-content {
    transition: height 0.35s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.3s ease;
  }
  .stat-card {
    opacity: 0;
    transform: translateY(32px);
    transition: opacity 1s ease, transform 1s ease;
  }
  .stat-card.revealed {
    opacity: 1;
    transform: translateY(0);
  }
  .stat-card:hover .stat-bg {
    transform: scale(1.08);
  }
  .eco-card:hover .eco-bg {
    transform: scale(1.08);
  }
</style>

<script>
  // Stats horizontal scroll — drag to scroll
  const statsScroll = document.getElementById('stats-scroll');
  if (statsScroll) {
    let isDown = false;
    let startX: number;
    let scrollLeft: number;

    statsScroll.addEventListener('mousedown', (e) => {
      isDown = true;
      statsScroll.style.cursor = 'grabbing';
      startX = e.pageX - statsScroll.offsetLeft;
      scrollLeft = statsScroll.scrollLeft;
    });
    statsScroll.addEventListener('mouseleave', () => { isDown = false; statsScroll.style.cursor = ''; resumeAuto(); });
    statsScroll.addEventListener('mouseup', () => { isDown = false; statsScroll.style.cursor = ''; resumeAuto(); });
    statsScroll.addEventListener('mousemove', (e) => {
      if (!isDown) return;
      e.preventDefault();
      const x = e.pageX - statsScroll.offsetLeft;
      statsScroll.scrollLeft = scrollLeft - (x - startX);
    });

    let autoInterval: ReturnType<typeof setInterval>;
    const scrollSpeed = 0.5;

    function startAuto() {
      autoInterval = setInterval(() => {
        if (isDown) return;
        statsScroll!.scrollLeft += scrollSpeed;
        if (statsScroll!.scrollLeft >= statsScroll!.scrollWidth - statsScroll!.clientWidth) {
          statsScroll!.scrollLeft = 0;
        }
      }, 16);
    }

    function pauseAuto() { clearInterval(autoInterval); }
    function resumeAuto() { pauseAuto(); startAuto(); }

    statsScroll.addEventListener('mouseenter', pauseAuto);
    statsScroll.addEventListener('touchstart', pauseAuto);
    statsScroll.addEventListener('touchend', () => { setTimeout(resumeAuto, 2000); });

    startAuto();
  }

  // Stats scroll reveal with stagger + trigger counters
  const statCards = document.querySelectorAll('.stat-card');
  const statsObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const card = entry.target as HTMLElement;
          const index = parseInt(card.dataset.statIndex || '0');
          const delay = index * 150;
          setTimeout(() => {
            card.classList.add('revealed');
            const counter = card.querySelector('.stat-counter') as HTMLElement;
            if (counter) {
              setTimeout(() => {
                animateCounter(counter, counter.dataset.target || '0');
              }, 400);
            }
          }, delay);
          statsObserver.unobserve(card);
        }
      });
    },
    { threshold: 0.05 }
  );
  statCards.forEach((card) => statsObserver.observe(card));

  function animateCounter(el: HTMLElement, target: string) {
    const match = target.match(/^([~]?)([\d.]+)(.*)$/);
    if (!match) { el.textContent = target; return; }

    const prefix = match[1];
    const endNum = parseFloat(match[2]);
    const suffix = match[3];
    const duration = 2000;
    const startTime = performance.now();

    function tick(now: number) {
      const elapsed = now - startTime;
      const progress = Math.min(elapsed / duration, 1);
      const eased = 1 - Math.pow(1 - progress, 3);
      const current = eased * endNum;

      if (endNum >= 10) {
        el.textContent = prefix + Math.round(current) + suffix;
      } else {
        el.textContent = prefix + current.toFixed(1).replace(/\.0$/, '') + suffix;
      }

      if (progress < 1) requestAnimationFrame(tick);
      else el.textContent = target;
    }

    requestAnimationFrame(tick);
  }

  // Service selector — desktop two-column
  const selectors = document.querySelectorAll('[data-svc-select]');
  const panels = document.querySelectorAll('[data-svc-panel]');
  let activeIndex = 0;
  let isAnimating = false;

  selectors.forEach((sel) => {
    sel.addEventListener('click', () => {
      const index = parseInt((sel as HTMLElement).dataset.svcSelect || '0');
      if (index === activeIndex || isAnimating) return;
      isAnimating = true;

      selectors.forEach((s) => {
        const el = s as HTMLElement;
        el.style.background = 'transparent';
        const ind = el.querySelector('.svc-sel-indicator') as HTMLElement;
        const ico = el.querySelector('.svc-sel-icon') as HTMLElement;
        const lbl = el.querySelector('.svc-sel-label') as HTMLElement;
        if (ind) ind.style.opacity = '0';
        if (ico) { ico.style.background = 'transparent'; ico.style.color = '#5C5C5C'; }
        if (lbl) lbl.style.color = '#5C5C5C';
      });

      const active = sel as HTMLElement;
      active.style.background = '#F7F2EE';
      const ind = active.querySelector('.svc-sel-indicator') as HTMLElement;
      const ico = active.querySelector('.svc-sel-icon') as HTMLElement;
      const lbl = active.querySelector('.svc-sel-label') as HTMLElement;
      if (ind) ind.style.opacity = '1';
      if (ico) { ico.style.background = '#E8E0D8'; ico.style.color = '#CC3333'; }
      if (lbl) lbl.style.color = '#1A1A1A';

      const oldPanel = panels[activeIndex] as HTMLElement;
      const newPanel = panels[index] as HTMLElement;
      const goingDown = index > activeIndex;

      oldPanel.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
      oldPanel.style.opacity = '0';
      oldPanel.style.transform = goingDown ? 'translateY(-20px)' : 'translateY(20px)';

      setTimeout(() => {
        oldPanel.style.display = 'none';
        oldPanel.style.transform = '';
        oldPanel.style.transition = '';

        newPanel.style.opacity = '0';
        newPanel.style.transform = goingDown ? 'translateY(20px)' : 'translateY(-20px)';
        newPanel.style.display = '';

        requestAnimationFrame(() => {
          newPanel.style.transition = 'opacity 0.35s ease, transform 0.35s ease';
          newPanel.style.opacity = '1';
          newPanel.style.transform = 'translateY(0)';

          setTimeout(() => {
            newPanel.style.transition = '';
            activeIndex = index;
            isAnimating = false;
          }, 350);
        });
      }, 280);
    });
  });

  // Ecosystem cards — tilt on hover
  document.querySelectorAll('[data-tilt-eco]').forEach((card) => {
    const el = card as HTMLElement;
    el.addEventListener('mousemove', (e: MouseEvent) => {
      const rect = el.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;
      const centerX = rect.width / 2;
      const centerY = rect.height / 2;
      const rotateX = ((y - centerY) / centerY) * -3;
      const rotateY = ((x - centerX) / centerX) * 3;
      el.style.transform = `perspective(800px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`;
    });
    el.addEventListener('mouseleave', () => {
      el.style.transform = 'perspective(800px) rotateX(0deg) rotateY(0deg) scale(1)';
    });
  });

  // FAQ accordion
  document.querySelectorAll('[data-faq]').forEach((item) => {
    const trigger = item.querySelector('[data-faq-trigger]') as HTMLButtonElement;
    const content = item.querySelector('.faq-content') as HTMLElement;
    if (!trigger || !content) return;

    trigger.addEventListener('click', () => {
      const isOpen = trigger.getAttribute('aria-expanded') === 'true';

      if (isOpen) {
        content.style.height = content.scrollHeight + 'px';
        requestAnimationFrame(() => {
          content.style.height = '0';
          content.style.opacity = '0';
        });
        trigger.setAttribute('aria-expanded', 'false');
        const ch = trigger.querySelector('.faq-chevron') as HTMLElement;
        if (ch) ch.style.transform = 'rotate(0deg)';
      } else {
        content.style.height = '0';
        requestAnimationFrame(() => {
          content.style.height = content.scrollHeight + 'px';
          content.style.opacity = '1';
        });
        trigger.setAttribute('aria-expanded', 'true');
        const ch = trigger.querySelector('.faq-chevron') as HTMLElement;
        if (ch) ch.style.transform = 'rotate(180deg)';
      }
    });
  });
</script>
</PageLayout>
