Create a new Astro page at `src/pages/$ARGUMENTS.astro`.
- Use `PageLayout` as the layout
- Include SEO meta tags (title, description, canonical, og:image)
- Follow design tokens from the design guide
- Create any new components needed in the appropriate directory
