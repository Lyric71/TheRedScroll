Create a new Astro component for: $ARGUMENTS
- Place in the correct subdirectory under `src/components/`
- Use TypeScript for the frontmatter Props interface
- Use Tailwind classes matching the design guide tokens
- Support responsive behaviour (mobile-first)
- Use semantic HTML elements
